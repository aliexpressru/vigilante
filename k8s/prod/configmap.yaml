apiVersion: v1
kind: ConfigMap
metadata:
  name: vigilante-config
  labels:
    app: vigilante
data:
  appsettings.json: |
    {
      "Qdrant": {
        "MonitoringIntervalSeconds": 30,
        "HttpTimeoutSeconds": 5,
        "EnableAutoRecovery": true,
        "ApiKey": "test"
      }
    }

